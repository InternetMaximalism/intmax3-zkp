# =============================================================================
# Rust / Cargo
# =============================================================================
/target
**/*.rs.bk
*.profraw
*.profdata

# =============================================================================
# Foundry / Solidity
# =============================================================================
contracts/cache/
contracts/out/
contracts/broadcast/
contracts/foundry.lock

# =============================================================================
# Test artifacts (binary fixtures, log noise)
# =============================================================================
tests/fixtures/*.bin
test_output.log
*.log

# WHIR Keccak256Chain fixture (regenerated from Rust tests)
contracts/test/data/whir/keccak256chain_verifier_data.json

# =============================================================================
# Stray submodule checkouts (from cross-branch work)
# =============================================================================
contracts/lib/polygon-plonky2/

# =============================================================================
# Node / WASM tooling
# =============================================================================
node_modules/
pkg/
dist/
build/
.npm/
.pnpm-store/

# WebDriver / TLS certs for browser tests
self_certs/

# =============================================================================
# Lean 4 / Lake build output (proof sources live in doc/architecture-audit/ and doc/audit/)
# =============================================================================
.lake/
*.olean
*.ilean
doc/architecture-audit/lake-manifest.json

# Stale pre-consolidation Lean project roots: the sources moved under doc/ (2026-07-05),
# but invoking `lake` from the old paths recreates these build-only trees.
/architecture-audit/
/audit/

# =============================================================================
# OS / editor noise
# =============================================================================
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
*.iml

# =============================================================================
# AI agents / local-only notes
# =============================================================================
# codex
AGENTS.md

# Claude Code internals (settings, cached agent state)
.claude/

# Secrets — testnet/Sepolia deployer private key and any private-key material.
# Explicit belt-and-suspenders on top of `.claude/` above: NEVER commit these.
.claude/priv
priv
*.priv
*.key
*.eth
.env
.env.*

# =============================================================================
# Browser-wallet e2e runtime scratch (JSON handoffs + CLI state)
wallet-e2e-work/
wallet-live-work/
wallet-live-work.*/
cli_state.json
# Persisted by `channel_member close` (CLOSE_INTENT_FULL_FILE) and consumed by
# `cancel-close` / `post-close-claim` in the SAME working directory. Runtime
# state, not a fixture — a committed copy is stale state a fresh clone reads.
close_intent_full.json

# =============================================================================
# Misc
# =============================================================================
tmp/
temp/
*.bak

# Sepolia/AWS deploy build artifacts (binary + cached backing — regenerated)
deploy-staging/
signer-bin/

# Server / deployment operational records (IPs, instance ids, live URLs, on-chain addrs)
deploy-record*.md
**/deploy-record.md
DEPLOYMENT.md
CLAUDE.md

# Local project instructions + vestigial gnark (Go Groth16 prover; removed from the
# active MLE pipeline, excluded from the Cargo workspace — kept locally, not tracked)
gnark/

# Local mainnet-readiness notes (not for tracking)
mainnetblocker.md
